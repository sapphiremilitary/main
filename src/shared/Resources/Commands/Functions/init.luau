--!strict

local module = {
	ff = require(script.FF),
	kick = require(script.Kick),
	refresh = require(script.Refresh),
	respawn = require(script.Respawn),
	team = require(script.Team),
	teleport = require(script.Teleport),
	unff = require(script.Unff),
}

return module
